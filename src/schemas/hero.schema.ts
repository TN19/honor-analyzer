import { z } from 'zod'
const lane = z.enum(['clash','jungle','mid','farm','roamer'])
const skill = z.object({id:z.string(),name:z.string(),type:z.enum(['active','passive']),description:z.string(),mechanics:z.array(z.string()),tags:z.array(z.string()),targeting:z.string(),damageType:z.string(),crowdControl:z.array(z.string()),mobility:z.array(z.string()),defensiveEffects:z.array(z.string()),offensiveEffects:z.array(z.string()),resourceEffects:z.array(z.string()),scalingNotes:z.array(z.string()),interactionNotes:z.array(z.string()),patch:z.string()})
export const heroSchema = z.object({id:z.string().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/),name:z.string().min(1),aliases:z.array(z.string()),image:z.string(),roles:z.array(z.string()),lanes:z.array(lane),primaryLane:lane.or(z.literal('unknown')),damage:z.object({primary:z.string(),secondary:z.string().nullable()}),tags:z.array(z.string()),strengths:z.array(z.string()),weaknesses:z.array(z.string()),gamePhases:z.object({early:z.string(),mid:z.string(),late:z.string()}),skills:z.record(z.string(),skill),archetypes:z.record(z.string(),z.number().min(0).max(10)).optional(),metaScore:z.number().min(0).max(10).optional(),patch:z.string(),updatedAt:z.string(),sources:z.array(z.unknown())})
